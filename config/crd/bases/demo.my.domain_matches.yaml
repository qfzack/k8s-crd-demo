---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: matches.demo.my.domain
spec:
  group: demo.my.domain
  names:
    kind: Match
    listKind: MatchList
    plural: matches
    singular: match
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Match is the Schema for the matches API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MatchSpec defines the desired state of Match
            properties:
              awayTeam:
                type: string
              awayTeamGoals:
                type: integer
              datetime:
                type: string
              homeTeam:
                type: string
              homeTeamGoals:
                type: integer
            required:
            - awayTeam
            - awayTeamGoals
            - datetime
            - homeTeam
            - homeTeamGoals
            type: object
          status:
            description: MatchStatus defines the observed state of Match
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
